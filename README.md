# Satyajeet Moharana — Academic Website Prototype

## Preview locally

Open `index.html` directly in a browser, or run:

```bash
python3 -m http.server 8000
```

Then visit `http://localhost:8000`.

## Next edits

- Replace placeholder `#` links for ORCID, ADS, Google Scholar, GitHub, and LinkedIn.
- Add DOI/ADS links to publications.
- Replace the annotated Serpens South PDF export with a clean, high-resolution map for the hero background.
- Build separate project and publication pages.
